import * as tlma from 'vsts-task-lib/mock-answer';
import * as tlmr from 'vsts-task-lib/mock-run';
import * as tlmt from 'vsts-task-lib/mock-test';

describe('AddBuildTag task', () => {
    before(() => { });
    it("It should tag the build");
    after(() => { });
});

describe('AttachSummaryMarkdown task', () => {
    before(() => { });
    it("It should attach summary markdown");
    after(() => { });
});

describe('LogVariables task', () => {
    before(() => { });
    it("It should predefined log variables");
    after(() => { });
});

describe('UpdateBuildNumber task', () => {
    before(() => { });
    it("It should update the build number");
    after(() => { });
});